<nav class="navbar navbar-expand-lg navbar-light bg-light mx-0 p-0" style="z-index:1000;"">
  <a href="<%= root_path %>" class="btn btn-primary p-2 m-2" >IOT BlackBox</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse justify-content-between" id="navbarNavDropdown">
    <ul class="navbar-nav">
    <% if user_signed_in? %>

      <li class="nav-item active">
        <a class="nav-link" href="<%= root_path %>">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= sensors_path %>">Sensors</a>
      </li>
      <li class="nav-item">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownFootage" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Footage
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdownFootage">
            <a class="dropdown-item" href="<%= videos_path %>"><span class="fe fe-folder"></span> recorded</a>
            <a class="dropdown-item" href="<%= streams_path %>"><span class="fe fe-video"></span> Stream</a>
          </div>
        </li>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Map
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
          <a class="dropdown-item" href="<%= maps_path %>"><span class="fe fe-clock"></span> History</a>
          <a class="dropdown-item" href="<%= maps_path + "/current" %>"><span class="fe fe-map"></span> Live Map</a>
        </div>
      </li>

    <% end %>
    </ul>
    <div class="btn-group dropleft float-right mr-4">
      <% if user_signed_in? %>
      <button type="button" class="btn btn-outline-secondary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <strong><%= current_user.username %></strong>
      </button>
      <div class="dropdown-menu">
        <%= link_to 'Edit profile', edit_user_registration_path, :class => 'dropdown-item' %>
        <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'dropdown-item'  %>
      </div>
      <% else %>
      <button type="button" class="btn btn-outline-secondary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <span class="fe fe-user"></span> Account
      </button>
      <div class="dropdown-menu">
        <%= link_to "Login", new_user_session_path, :class => 'dropdown-item'  %>
        <%= link_to "Sign up", new_user_registration_path, :class => 'dropdown-item'  %>
      </div>
      <% end %>
    </div>
  </div>
</nav>
